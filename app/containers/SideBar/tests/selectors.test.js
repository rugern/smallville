// import { fromJS } from 'immutable';
// import { makeSelectSidebarDomain } from '../selectors';

// const selector = makeSelectSidebarDomain();

describe('makeSelectSidebarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
