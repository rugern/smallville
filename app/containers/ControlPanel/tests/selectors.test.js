// import { fromJS } from 'immutable';
// import { makeSelectControlPanelDomain } from '../selectors';

// const selector = makeSelectControlPanelDomain();

describe('makeSelectControlPanelDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
