// import { fromJS } from 'immutable';
// import { makeSelectMainDomain } from '../selectors';

// const selector = makeSelectMainDomain();

describe('makeSelectMainDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
